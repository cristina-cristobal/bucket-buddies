<h1>Details about Goal</h1>

Goal Name: <%= @goal.title %><br>
People trying to accomplish this goal:
<ul>
  <% @goal.users.each do |user| %>
  <li><%= link_to user.username, user_path(user) %></li>
  <% end %>
</ul>

<%= link_to "Like This Goal", add_path(@goal) %>
